language: go
go:
    - 1.9

before_install:
    - sudo apt-get install python-guessit

install:
    - go get -u github.com/golang/dep/cmd/dep
    - dep ensure

before_script:
    - go vet ./...

script:
    - ./test.sh

after_success:
  - bash <(curl -s https://codecov.io/bash)
