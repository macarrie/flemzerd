language: go
go:
    - 1.9

before_install:
    - sudo pip install guessit

install:
    - go get -u github.com/golang/dep/cmd/dep
    - make deps

before_script:
    - go vet ./...

script:
    - make test

after_success:
  - bash <(curl -s https://codecov.io/bash)
